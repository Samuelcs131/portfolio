import { iThemeColor } from "./theme";

// STYLES
export interface GlobalStyle {
    theme: iThemeColor
}

export interface iSkillsStyle {
    name: string,
    color: string
}

export interface iProjectsStyle {
    image: string
}